name: gym_log
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.3

dependencies:
  audioplayers: ^6.8.1
  dio: ^5.11.1
  fl_chart: ^1.2.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.4.3
  flutter_secure_storage: ^11.2.0
  flutter_tts: ^4.2.5
  shared_preferences: ^2.5.5
  wakelock_plus: ^1.8.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # 计时提示音。分频编码：不同音高 = 不同含义（TIMER-SPEC 3.4）
  assets:
    - assets/audio/
